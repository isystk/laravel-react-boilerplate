import React, { VFC } from "react";
import Layout from "@/components/Layout";

const NotFound: VFC = () => {
    return (
        <Layout>
            <main className="main">
                <h1>Not Found</h1>;
            </main>
        </Layout>
    );
};

export default NotFound;
